services:
  - type: cron
    name: gold-bot
    schedule: "*/2 * * * *"
    runtime: python3
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: python gold_bot.py
